import { Area, AreaChart, Bar, BarChart, ResponsiveContainer, Tooltip, XAxis, YAxis, CartesianGrid, LabelList } from "recharts";
import { DollarSign, Ticket, Clock, Flame, Thermometer, QrCode, AlertTriangle, RefreshCw } from "lucide-react";
import type { ReactNode } from "react";
import { cn } from "@/lib/utils";
import { Link } from "@tanstack/react-router";
import { useOperationalEvent } from "@/lib/events-queries";
import { useSales, useSalesStats, formatCurrency, type Sale } from "@/lib/sales-queries";
import { useHourlySalesStats, useAudienceStats, usePixFailures, classifyTemperature, type TemperatureLevel } from "@/lib/dashboard-queries";
import { useOperationalPreferences } from "@/lib/settings-queries";
import { useAuth } from "@/lib/auth-context";
import { DashboardMetricCard as MetricCard } from "@/components/admin/DashboardMetricCard";

const TEMPERATURE_META: Record<TemperatureLevel, { label: string; icon: any; color: string }> = {
  normal: { label: "Fria", icon: Thermometer, color: "text-sky-400" },
  aquecendo: { label: "Aquecendo", icon: Thermometer, color: "text-warning" },
  quente: { label: "Quente", icon: Thermometer, color: "text-error" },
  explodindo: { label: "Explodindo", icon: Flame, color: "text-error" },
};

const Panel = ({ title, children, action }: { title: string; children: ReactNode; action?: ReactNode }) => <section className="flex h-full flex-col rounded-[var(--radius-md)] bg-bg-secondary p-6"><div className="mb-5 flex items-center justify-between gap-4"><h2 className="text-heading-2 text-text-primary">{title}</h2>{action}</div><div className="flex-1">{children}</div></section>;

export function AdminDashboard() {
  const { loading: authLoading, refreshProfile } = useAuth();
  const { isLoading: eventsLoading, error: eventsError, event: operationalEvent, refetch: refetchEvents } = useOperationalEvent();
  const eventId = operationalEvent?.id;
  const operationalEventId = operationalEvent?.id ?? null;
  const { data: stats, isLoading: statsLoading, error: statsError, refetch: refetchStats } = useSalesStats(operationalEventId);
  const { data: sales = [], isLoading: salesLoading, error: salesError, refetch: refetchSales } = useSales(operationalEventId);
  const { data: hourlyData = [], isLoading: hourlyLoading, error: hourlyError, refetch: refetchHourly } = useHourlySalesStats(operationalEventId);
  const { data: audience, error: audienceError, refetch: refetchAudience } = useAudienceStats(operationalEventId);
  const { data: pixFailures, error: pixFailuresError, refetch: refetchPixFailures } = usePixFailures(operationalEventId);
  const { data: preferences, isLoading: preferencesLoading, error: preferencesError, refetch: refetchPreferences } = useOperationalPreferences();
  const scopedSales = sales;
  const lastSales = scopedSales.filter((sale: any) => !sale.is_courtesy).slice(0, 8);
  const eventStarted = operationalEvent ? new Date(operationalEvent.event_date).getTime() <= Date.now() : false;
  const recentPaidSales = scopedSales
    .filter((sale: any) => !sale.is_courtesy && sale.status === "pago" && new Date(sale.created_at).getTime() >= Date.now() - 24 * 3600_000)
    .reduce((sum: number, sale: any) => sum + (sale.quantity || 0), 0);
  const thresholds = preferences ? { aquecendo: preferences.temperature_aquecendo_sales_per_day, quente: preferences.temperature_quente_sales_per_day, explodindo: preferences.temperature_explodindo_sales_per_day } : { aquecendo: 10, quente: 25, explodindo: 50 };
  const temperatureMeta = TEMPERATURE_META[classifyTemperature(recentPaidSales, thresholds)];
  const blockingError = eventsError || statsError || salesError || hourlyError || preferencesError;
  const handleRetry = async () => {
    // Reconfirma sessão/organização primeiro — se a causa foi uma falha
    // momentânea ali (o caso mais comum), as consultas abaixo já saem
    // certas na sequência. Só repeti-las, sem isso, repetia o mesmo erro.
    await refreshProfile();
    void refetchEvents();
    void refetchStats();
    void refetchSales();
    void refetchHourly();
    void refetchAudience();
    void refetchPixFailures();
    void refetchPreferences();
  };

  if (authLoading || eventsLoading || statsLoading || salesLoading || hourlyLoading || preferencesLoading) return <div className="flex min-h-[400px] flex-col items-center justify-center gap-3"><span className="animate-spin"><Clock className="h-6 w-6" /></span><span>Carregando métricas...</span></div>;

  if (!operationalEvent) return <div className="flex min-h-[400px] flex-col items-center justify-center gap-4 px-6 text-center"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-bg-secondary"><Ticket className="h-6 w-6 text-text-secondary" /></div><div><h2 className="text-heading-2 text-text-primary">Nenhum evento ativo</h2><p className="mt-1 max-w-xl text-small text-text-secondary">Crie ou publique um novo evento para começar a operação.</p></div><Link to="/admin/eventos" className="rounded-[var(--radius-sm)] bg-accent px-4 py-2 text-small font-semibold text-[var(--accent-foreground)]">Ir para Eventos</Link></div>;

  if (blockingError) return <div className="flex min-h-[400px] flex-col items-center justify-center gap-4 px-6 text-center"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-error/10"><AlertTriangle className="h-6 w-6 text-error" /></div><div><h2 className="text-heading-2 text-text-primary">Não foi possível carregar as métricas</h2><p className="mt-1 max-w-xl text-small text-text-secondary">A sessão ou uma consulta administrativa não respondeu corretamente. Seus dados não foram apagados.</p><p className="mt-2 max-w-xl break-words text-small text-error">{blockingError instanceof Error ? blockingError.message : String(blockingError)}</p></div><button type="button" onClick={handleRetry} className="inline-flex items-center gap-2 rounded-[var(--radius-sm)] bg-bg-secondary px-4 py-2 text-small font-semibold text-text-primary shadow-sm hover:bg-bg-tertiary"><RefreshCw className="h-4 w-4" />Tentar novamente</button></div>;

  return <div className="space-y-6">
    <div><p className="text-small text-text-secondary">{operationalEvent ? "Evento atual" : "Operação"}</p><h1 className="text-heading-1 text-text-primary">{operationalEvent?.title ?? "Nenhum evento ativo"}</h1></div>
    {!!pixFailures?.count && <div className="flex flex-wrap items-center gap-3 rounded-[var(--radius-md)] border border-error/40 bg-error/10 px-4 py-3"><AlertTriangle className="h-5 w-5 shrink-0 text-error" /><p className="flex-1 text-body text-text-primary"><span className="font-semibold">{pixFailures.count} {pixFailures.count === 1 ? "falha recente" : "falhas recentes"} na geração de Pix</span> <span className="text-text-secondary">nas últimas 24h — verifique o Mercado Pago.</span></p><Link to="/admin/configuracoes/mercado-pago" className="text-small font-semibold text-accent hover:underline">Verificar configuração →</Link></div>}
    {!!audienceError && <div className="flex items-center gap-2 rounded-[var(--radius-md)] border border-warning/40 bg-warning/10 px-4 py-3 text-small text-text-primary"><AlertTriangle className="h-4 w-4 shrink-0" />Dados do público indisponíveis no momento.</div>}
    {!!pixFailuresError && <div className="flex items-center gap-2 rounded-[var(--radius-md)] border border-warning/40 bg-warning/10 px-4 py-3 text-small text-text-primary"><AlertTriangle className="h-4 w-4 shrink-0" />Não foi possível consultar as falhas recentes de Pix.</div>}
    <div className="grid grid-cols-1 items-stretch gap-4 md:grid-cols-2 lg:grid-cols-4"><MetricCard title="Receita" value={formatCurrency(stats?.totalRevenue || 0)} icon={DollarSign} trend={stats?.paidSales ? `Baseado em ${stats.paidSales} vendas` : "Nenhuma venda paga"} iconColor="text-icon-brand" /><MetricCard title="Ingressos" value={stats?.totalTickets || 0} icon={Ticket} secondary="ingressos confirmados" iconColor="text-icon-brand" />{eventStarted ? <MetricCard title="Check-in" value={stats?.checkins || 0} icon={QrCode} secondary={stats?.validTickets ? `de ${stats.validTickets} ingressos válidos` : "nenhum ingresso válido"} gaugeValue={stats?.validTickets ? Math.round(((stats?.checkins || 0) / stats.validTickets) * 100) : 0} iconColor="text-icon-brand" /> : <MetricCard title="Pendentes" value={stats?.pendingSales || 0} icon={Clock} secondary={`${formatCurrency(stats?.pendingAmount || 0)} aguardando pagamento`} gaugeValue={stats?.totalSales ? Math.round((stats.pendingSales / stats.totalSales) * 100) : 0} iconColor="text-icon-brand" />}<MetricCard title="Temperatura" value={temperatureMeta.label} icon={temperatureMeta.icon} iconColor={temperatureMeta.color} iconSize="h-16 w-16" secondary={`${recentPaidSales} ingressos nas últimas 24h`} temperatureUnit /></div>
    <div className="grid grid-cols-1 items-stretch gap-4 lg:grid-cols-2"><Panel title="Vendas diárias (últimos 14 dias)"><div className="h-[300px] w-full"><ResponsiveContainer width="100%" height="100%"><AreaChart data={stats?.last14Days || []} margin={{ top: 10, right: 10, left: -20, bottom: 0 }}><defs><linearGradient id="colorSales" x1="0" y1="0" x2="0" y2="1"><stop offset="5%" stopColor="var(--accent)" stopOpacity={0.15}/><stop offset="95%" stopColor="var(--accent)" stopOpacity={0}/></linearGradient></defs><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border-subtle)" /><XAxis dataKey="date" axisLine={false} tickLine={false} tick={{ fontSize: 12, fill: "var(--text-secondary)" }} dy={10} /><YAxis axisLine={false} tickLine={false} tick={{ fontSize: 12, fill: "var(--text-secondary)" }} /><Tooltip contentStyle={{ backgroundColor: "var(--bg-secondary)", borderColor: "var(--border-default)", borderRadius: "var(--radius-sm)", color: "var(--text-primary)" }} itemStyle={{ color: "var(--accent)" }} formatter={((value: number) => [`${value} ingresso${value === 1 ? "" : "s"}`, "Vendas"]) as any} /><Area type="monotone" dataKey="value" stroke="var(--accent)" strokeWidth={1} fillOpacity={1} fill="url(#colorSales)" /></AreaChart></ResponsiveContainer></div></Panel><Panel title="Picos de venda por horário"><div className="h-[300px] w-full"><ResponsiveContainer width="100%" height="100%"><BarChart data={hourlyData} margin={{ top: 10, right: 10, left: -20, bottom: 0 }} barSize={32}><CartesianGrid strokeDasharray="3 3" vertical={false} stroke="var(--border-subtle)" /><XAxis dataKey="hour" axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: "var(--text-secondary)" }} dy={10} /><YAxis allowDecimals={false} axisLine={false} tickLine={false} tick={{ fontSize: 12, fill: "var(--text-secondary)" }} width={30} /><Tooltip cursor={{ fill: "var(--bg-tertiary)" }} contentStyle={{ backgroundColor: "var(--bg-secondary)", borderColor: "var(--border-default)", borderRadius: "var(--radius-sm)" }} labelFormatter={(hour) => `Faixa das ${hour}`} formatter={((value: number) => [`${value} ingresso${value === 1 ? "" : "s"}`, "Vendas"]) as any} /><Bar dataKey="value" radius={[6, 6, 0, 0]} fill="var(--accent)" className="opacity-80 hover:opacity-100"><LabelList dataKey="value" position="top" formatter={((value: number) => (value > 0 ? value : "")) as any} style={{ fill: "var(--text-secondary)", fontSize: 11, fontWeight: 600 }} /></Bar></BarChart></ResponsiveContainer></div></Panel></div>
    <div className="grid grid-cols-1 items-stretch gap-4 lg:grid-cols-2"><Panel title="Últimas vendas" action={<Link to="/admin/vendas" className="text-small text-accent hover:underline">Ver todas →</Link>}><div className="max-h-[320px] divide-y divide-border-subtle overflow-y-auto">{lastSales.map((sale: Sale) => { const quantity = Number(sale.quantity) || 0; const isPaid = sale.status === "pago"; const actionLabel = isPaid ? `comprou ${quantity} ${quantity === 1 ? "ingresso" : "ingressos"}` : `solicitou uma compra de ${quantity} ${quantity === 1 ? "ingresso" : "ingressos"}...`; const dateLabel = new Date(sale.created_at).toLocaleString("pt-BR", { day: "2-digit", month: "2-digit", hour: "2-digit", minute: "2-digit", timeZone: "America/Sao_Paulo" }); const initials = sale.buyer_name.trim().split(/\s+/).slice(0, 2).map((n: string) => n[0]).join("").toUpperCase(); return <div key={sale.id} className="flex items-center gap-4 py-3"><div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-full bg-bg-tertiary text-small font-semibold text-text-primary">{initials}</div><div className="flex-1 text-small text-text-primary"><span className="font-medium">{sale.buyer_name}</span> <span className="text-text-secondary">{actionLabel}</span></div><div className="shrink-0 text-small text-text-secondary">{dateLabel}</div></div>; })}{lastSales.length === 0 && <div className="py-8 text-center text-text-secondary">Nenhuma venda registrada.</div>}</div></Panel><Panel title="Dados do público"><div className="flex flex-col divide-y divide-border-subtle"><div className="flex items-center justify-between py-2.5 text-body"><span className="text-text-secondary">Visitas ao evento</span><span className="font-semibold text-text-primary">{audience?.totalViews ?? "—"}</span></div><div className="flex items-center justify-between py-2.5 text-body"><span className="text-text-secondary">Idade média / Faixa etária</span><span className="font-semibold text-text-primary">{audience?.averageAge && audience?.ageRange ? `${audience.averageAge} Anos / ${audience.ageRange.min} - ${audience.ageRange.max}` : "—"}</span></div><div className="flex items-center justify-between py-2.5 text-body"><span className="text-text-secondary">Sexo predominante</span><span className="font-semibold text-text-primary">{audience?.genderSplit ? `Feminino ${audience.genderSplit.femalePct}% - Masculino ${audience.genderSplit.malePct}%` : "—"}</span></div><div className="flex items-center justify-between py-2.5 text-body"><span className="text-text-secondary">Clientes recorrentes</span><span className="font-semibold text-text-primary">{audience?.recurringCustomers ?? "—"}</span></div><div className="flex items-start justify-between py-2.5 text-body"><span className="text-text-secondary">Principais cidades</span><span className="text-right font-semibold text-text-primary">{audience?.topCities?.length ? audience.topCities.map((c) => <div key={c.city}>{c.city} ({c.count})</div>) : "—"}</span></div></div></Panel></div>
  </div>;
}
